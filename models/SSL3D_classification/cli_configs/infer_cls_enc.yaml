hydra:
  output_subdir: null
  job:
    chdir: False
  run:
    dir: .

override hydra/hydra_logging: disable
override hydra/job_logging: disable

exp_dir: /bdm-das/ADSP_v1/nnssl_dataset/nnssl_results/ADNI/DX_class/ADNI_ResEnc_DX_class_20_DblWarmup_lr_1e-3/DX_cls/checkpoints # path to the directory containing the hydra configs and fold directories
ckpt_dir: /bdm-das/ADSP_v1/nnssl_dataset/nnssl_results/ADNI/DX_class/ADNI_ResEnc_DX_class_20_DblWarmup_lr_1e-3/DX_cls/checkpoints/bfc7c20e-e926-4c38-84dc-2bc229995b4e # Parent directory of fold 0
fold: 0  # if no fold is given, an ensemble of all available folds will be used (if only one fold is available this can be ignored)
pred_dir: null

metrics:
  - 'f1'
  - 'balanced_acc'
  - 'ap'
  - 'auroc'