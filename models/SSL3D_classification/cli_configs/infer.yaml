hydra:
  output_subdir: null
  job:
    chdir: False
  run:
    dir: .

override hydra/hydra_logging: disable
override hydra/job_logging: disable

exp_dir: /bdm-das/ADSP_v1/nnssl_dataset/nnssl_results/UKBB/UKBB_ResEnc_no_pretrained_ord_reg_lr_1e-4_no_Warmup/AgeReg/checkpoints # path to the directory containing the hydra configs and fold directories
ckpt_dir: /bdm-das/ADSP_v1/nnssl_dataset/nnssl_results/UKBB/UKBB_ResEnc_no_pretrained_ord_reg_lr_1e-4_no_Warmup/AgeReg/checkpoints/5c6f3ccb-da22-4583-a65e-6d57ff198d78 # Parent directory of fold 0
fold: 0  # if no fold is given, an ensemble of all available folds will be used (if only one fold is available this can be ignored)
pred_dir: null

metrics:
  - 'mae'
  - 'mse'